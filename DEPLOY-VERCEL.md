# 🚀 Deploy ไปยัง Vercel

## ขั้นตอนที่ 1: เตรียมโปรเจค

### ตรวจสอบไฟล์ที่จำเป็น
- ✅ `app.py` - แอปพลิเคชัน Flask
- ✅ `vercel.json` - การตั้งค่า Vercel
- ✅ `requirements.txt` - Python dependencies
- ✅ `.vercelignore` - ไฟล์ที่ไม่ต้อง deploy
- ✅ `.gitignore` - ไฟล์ที่ไม่ต้อง commit

### ตรวจสอบว่าทุกอย่างทำงาน
```bash
# ทดสอบ local ก่อน
python app.py

# เปิด browser ไปที่ http://localhost:5000
# ทดสอบค้นหาและสร้างรายงาน
```

---

## ขั้นตอนที่ 2: Push ขึ้น GitHub

### 2.1 สร้าง Repository ใหม่บน GitHub
1. ไปที่ https://github.com/new
2. ตั้งชื่อ repository (เช่น `trade-in-system`)
3. เลือก **Private** (ถ้าไม่ต้องการให้คนอื่นเห็น)
4. **ไม่ต้อง** เลือก Initialize with README
5. คลิก **Create repository**

### 2.2 Push โค้ดขึ้น GitHub
```bash
# เข้าไปในโฟลเดอร์โปรเจค
cd /path/to/your/project

# Initialize git (ถ้ายังไม่ได้ทำ)
git init

# เพิ่มไฟล์ทั้งหมด
git add .

# Commit
git commit -m "Initial commit - Trade-In System"

# เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME และ YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

---

## ขั้นตอนที่ 3: Deploy บน Vercel

### 3.1 สร้างบัญชี Vercel
1. ไปที่ https://vercel.com/signup
2. เลือก **Continue with GitHub**
3. อนุญาตให้ Vercel เข้าถึง GitHub

### 3.2 Import Project
1. ไปที่ https://vercel.com/new
2. เลือก **Import Git Repository**
3. เลือก repository ที่สร้างไว้
4. คลิก **Import**

### 3.3 Configure Project
1. **Project Name**: `trade-in-system` (หรือชื่อที่ต้องการ)
2. **Framework Preset**: Other
3. **Root Directory**: `./` (ค่า default)
4. **Build Command**: (ปล่อยว่าง)
5. **Output Directory**: (ปล่อยว่าง)
6. **Install Command**: `pip install -r requirements.txt`

### 3.4 Environment Variables (ถ้ามี)
ถ้ามีค่า sensitive (เช่น API keys) ให้เพิ่มที่นี่:
- คลิก **Environment Variables**
- เพิ่มตัวแปรที่ต้องการ

### 3.5 Deploy!
1. คลิก **Deploy**
2. รอ 1-2 นาที
3. เสร็จแล้ว! 🎉

---

## ขั้นตอนที่ 4: ทดสอบ

### 4.1 เปิดเว็บไซต์
Vercel จะให้ URL แบบนี้:
```
https://trade-in-system.vercel.app
```

### 4.2 ทดสอบฟีเจอร์
- ✅ ค้นหาข้อมูล
- ✅ สร้างรายงาน
- ✅ ส่งไป Telegram
- ✅ ยกเลิกรายการ

---

## ขั้นตอนที่ 5: ตั้งค่าเพิ่มเติม

### 5.1 Custom Domain (ถ้าต้องการ)
1. ไปที่ Project Settings → Domains
2. เพิ่ม domain ของคุณ
3. ตั้งค่า DNS ตามที่ Vercel แนะนำ

### 5.2 Environment Variables
ถ้าต้องการเปลี่ยนค่า:
1. ไปที่ Project Settings → Environment Variables
2. เพิ่ม/แก้ไขตัวแปร
3. Redeploy

---

## 🔄 อัพเดทโค้ด

เมื่อแก้ไขโค้ด ให้ push ขึ้น GitHub:

```bash
# แก้ไขโค้ด
# ...

# Commit และ Push
git add .
git commit -m "Update: description of changes"
git push

# Vercel จะ deploy อัตโนมัติ!
```

---

## ⚠️ ข้อควรระวัง

### 1. Timeout 10 วินาที
- ถ้า API ตอบช้า อาจ timeout
- แก้ไข: ลดจำนวนข้อมูลที่ดึง หรือใช้ pagination

### 2. Cold Start
- ครั้งแรกที่เข้าอาจช้า 2-3 วินาที
- ปกติแล้วไม่ใช่ปัญหา

### 3. Session ID
- ต้องตั้งค่า Session ID ใหม่ทุกครั้งที่ deploy
- เก็บไว้ใน localStorage ของ browser

### 4. BRANCH_ID
- ตรวจสอบว่า `BRANCH_ID` ใน `app.py` ถูกต้อง
- ปัจจุบันตั้งเป็น `"231"`

---

## 🐛 Troubleshooting

### ปัญหา: Deploy ล้มเหลว
**แก้ไข:**
1. ตรวจสอบ `requirements.txt` ว่ามี dependencies ครบ
2. ตรวจสอบ `vercel.json` ว่าถูกต้อง
3. ดู logs ใน Vercel Dashboard

### ปัญหา: 500 Internal Server Error
**แก้ไข:**
1. ดู logs ใน Vercel Dashboard → Deployments → View Function Logs
2. ตรวจสอบ API endpoint ว่าทำงานถูกต้อง
3. ทดสอบ local ก่อน

### ปัญหา: Timeout
**แก้ไข:**
1. ลดจำนวนข้อมูลที่ดึง
2. เพิ่ม timeout protection (ทำไว้แล้ว)
3. พิจารณาใช้ Railway แทน

### ปัญหา: ไม่เจอข้อมูล
**แก้ไข:**
1. ตรวจสอบ `BRANCH_ID` ใน `app.py`
2. ตรวจสอบ Session ID
3. ตรวจสอบว่า login eve.techswop.com แล้ว

---

## 📊 Monitoring

### ดู Logs
1. ไปที่ Vercel Dashboard
2. เลือก Project
3. คลิก Deployments
4. เลือก deployment ล่าสุด
5. คลิก View Function Logs

### ดู Analytics
1. ไปที่ Project Settings → Analytics
2. ดูจำนวน requests, response time, errors

---

## 🎯 Next Steps

หลัง deploy สำเร็จแล้ว:

1. ✅ แชร์ URL ให้ทีม
2. ✅ ตั้งค่า Telegram Bot
3. ✅ ทดสอบส่งรายงาน
4. ✅ ตั้งค่า GitHub Actions สำหรับส่งอัตโนมัติ (ถ้าต้องการ)

---

## 📚 เอกสารเพิ่มเติม

- Vercel Docs: https://vercel.com/docs
- Python on Vercel: https://vercel.com/docs/functions/serverless-functions/runtimes/python
- Vercel CLI: https://vercel.com/docs/cli

---

## 🆘 ต้องการความช่วยเหลือ?

ถ้ามีปัญหา:
1. ดู logs ใน Vercel Dashboard
2. ตรวจสอบ GitHub Issues
3. ติดต่อทีมพัฒนา

---

**สำเร็จแล้ว! 🎉**

ระบบ Trade-In ของคุณพร้อมใช้งานบน Vercel แล้ว!
