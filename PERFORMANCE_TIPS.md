# ⚡ คำแนะนำการใช้งานสำหรับข้อมูลจำนวนมาก

## ปัญหา Timeout เมื่อดึงรายงานทั้งปี

### สาเหตุ

- **Vercel Free Tier**: จำกัดเวลา execution ที่ 10 วินาที
- **ข้อมูลจำนวนมาก**: ดึงข้อมูลทั้งปีอาจมีหลายหมื่นรายการ
- **API Rate Limit**: การเรียก API ภายนอกหลายครั้งใช้เวลานาน

### วิธีแก้ปัญหา

#### 1. แบ่งช่วงเวลาออกเป็นช่วงเล็กๆ (แนะนำ)

แทนที่จะดึงทั้งปี ให้แบ่งเป็น:

**ดึงทีละเดือน:**
```
มกราคม 2024: 01/01/2024 - 31/01/2024
กุมภาพันธ์ 2024: 01/02/2024 - 29/02/2024
มีนาคม 2024: 01/03/2024 - 31/03/2024
...
```

**ดึงทีละไตรมาส:**
```
Q1 2024: 01/01/2024 - 31/03/2024
Q2 2024: 01/04/2024 - 30/06/2024
Q3 2024: 01/07/2024 - 30/09/2024
Q4 2024: 01/10/2024 - 31/12/2024
```

#### 2. ใช้ Local Development (ไม่มี Timeout)

รัน Flask app บนเครื่องตัวเอง:

```bash
python3 app.py
```

จากนั้นเปิด http://localhost:5001

**ข้อดี:**
- ไม่มีข้อจำกัดเวลา (timeout 50 วินาที)
- ดึงข้อมูลได้สูงสุด 50,000 รายการ
- เร็วกว่าเพราะไม่ผ่าน Vercel

#### 3. Export ข้อมูลเป็น Excel แทน

ถ้าต้องการข้อมูลทั้งปี ให้:
1. ดึงข้อมูลทีละเดือน
2. Export เป็น Excel แต่ละเดือน
3. รวมไฟล์ Excel ทั้งหมดใน Excel/Google Sheets

#### 4. ใช้ Zone แทนการดึงทุกสาขา

ถ้าต้องการดูหลายสาขา:
- สร้าง Zone รวมสาขาที่ต้องการ
- ระบบจะดึงข้อมูลทีละสาขาแล้วรวมกัน
- แสดงความคืบหน้าระหว่างดึงข้อมูล

## ข้อจำกัดปัจจุบัน

### Vercel (Production)
- **Max Execution Time**: 10 วินาที
- **Effective Time**: 8 วินาที (เหลือ 2 วินาทีสำหรับ process)
- **Max Items**: 10,000 รายการ
- **Batch Size**: 1,000 รายการต่อครั้ง

### Local Development
- **Max Execution Time**: 50 วินาที
- **Max Items**: 50,000 รายการ
- **Batch Size**: 1,000 รายการต่อครั้ง

## คำแนะนำตามช่วงเวลา

| ช่วงเวลา | จำนวนวันโดยประมาณ | คำแนะนำ |
|---------|-------------------|---------|
| 1-7 วัน | 7 วัน | ✅ ใช้งานได้ปกติ |
| 1-30 วัน | 30 วัน | ✅ ใช้งานได้ดี |
| 1-90 วัน | 90 วัน | ⚠️ อาจใช้เวลา 30-60 วินาที |
| 3-6 เดือน | 180 วัน | ⚠️ แนะนำใช้ Local หรือแบ่งช่วง |
| 6-12 เดือน | 365 วัน | ❌ ต้องแบ่งช่วงหรือใช้ Local |

## วิธีดึงข้อมูลทั้งปีอย่างมีประสิทธิภาพ

### วิธีที่ 1: ดึงทีละเดือนแล้วรวมใน Excel

```bash
# 1. ดึงข้อมูลแต่ละเดือน
มกราคม: 01/01/2024 - 31/01/2024 → Export Excel
กุมภาพันธ์: 01/02/2024 - 29/02/2024 → Export Excel
...

# 2. รวมไฟล์ Excel ทั้งหมด
- เปิดไฟล์แรกใน Excel
- Copy ข้อมูลจากไฟล์อื่นๆ มาต่อท้าย
- หรือใช้ Power Query ใน Excel
```

### วิธีที่ 2: ใช้ Local Development

```bash
# 1. รัน Flask app บนเครื่อง
python3 app.py

# 2. เปิด http://localhost:5001

# 3. ดึงข้อมูลทั้งปีได้เลย (ใช้เวลา 1-2 นาที)
01/01/2024 - 31/12/2024

# 4. Export Excel
```

### วิธีที่ 3: ใช้ Script Python ดึงข้อมูลโดยตรง

สร้างไฟล์ `fetch_yearly_report.py`:

```python
import requests
import json
from datetime import datetime, timedelta
import pandas as pd

def fetch_monthly_data(year, month, session_id, branch_id='231'):
    """ดึงข้อมูลทีละเดือน"""
    # คำนวณวันแรกและวันสุดท้ายของเดือน
    first_day = datetime(year, month, 1)
    if month == 12:
        last_day = datetime(year, 12, 31)
    else:
        last_day = datetime(year, month + 1, 1) - timedelta(days=1)
    
    date_start = first_day.strftime('%d/%m/%Y')
    date_end = last_day.strftime('%d/%m/%Y')
    
    print(f"📅 Fetching {year}-{month:02d}: {date_start} - {date_end}")
    
    params = {
        'branchId': branch_id,
        'dateStart': date_start,
        'dateEnd': date_end,
        'sessionId': session_id,
        'saleCode': '',
        'customerSign': ''
    }
    
    response = requests.get('http://localhost:5001/api/report', params=params)
    data = response.json()
    
    if 'details' in data:
        print(f"   ✅ Got {len(data['details'])} items")
        return data['details']
    else:
        print(f"   ❌ Error: {data.get('error', 'Unknown error')}")
        return []

def fetch_yearly_report(year, session_id, branch_id='231'):
    """ดึงข้อมูลทั้งปี"""
    all_data = []
    
    for month in range(1, 13):
        monthly_data = fetch_monthly_data(year, month, session_id, branch_id)
        all_data.extend(monthly_data)
    
    print(f"\n✅ Total items: {len(all_data)}")
    return all_data

def export_to_excel(data, filename):
    """Export เป็น Excel"""
    df = pd.DataFrame(data)
    df.to_excel(filename, index=False)
    print(f"💾 Saved to {filename}")

if __name__ == '__main__':
    # ตั้งค่า
    YEAR = 2024
    SESSION_ID = 'your-session-id'  # ใส่ Session ID ของคุณ
    BRANCH_ID = '231'  # ใส่ Branch ID ที่ต้องการ
    
    # ดึงข้อมูล
    data = fetch_yearly_report(YEAR, SESSION_ID, BRANCH_ID)
    
    # Export
    filename = f'trade_in_report_{YEAR}.xlsx'
    export_to_excel(data, filename)
```

รัน:
```bash
pip3 install pandas openpyxl
python3 fetch_yearly_report.py
```

## Tips เพิ่มเติม

### 1. ใช้ Session ID ที่ถูกต้อง
- Session ID หมดอายุทุก 20 นาที
- ต้อง login ที่ eve.techswop.com ก่อนดึงข้อมูล
- ใช้ Extension ดึง Session ID อัตโนมัติ

### 2. ดึงข้อมูลในช่วงเวลาที่เหมาะสม
- **เช้า (8:00-10:00)**: ระบบยังไม่ค่อยมีคนใช้
- **กลางคืน (22:00-06:00)**: ระบบว่างที่สุด
- **หลีกเลี่ยง**: เวลาทำงาน (10:00-17:00)

### 3. ตรวจสอบข้อมูลก่อนดึง
- ดูจำนวนรายการโดยประมาณที่หน้าค้นหา
- ถ้ามากกว่า 5,000 รายการ ควรแบ่งช่วง

### 4. ใช้ Filter ลดข้อมูล
- กรอง Sale Code เฉพาะคนที่ต้องการ
- เลือกเฉพาะสถานะที่สนใจ
- เลือกเฉพาะแบรนด์ที่ต้องการ

## การ Deploy แบบไม่มี Timeout

### Option 1: Vercel Pro ($20/month)
- Timeout: 60 วินาที
- เพียงพอสำหรับข้อมูลทั้งปี

### Option 2: Railway / Render (Free Tier)
- Timeout: 300 วินาที (5 นาที)
- เพียงพอสำหรับข้อมูลหลายปี

### Option 3: VPS (DigitalOcean, Linode)
- ไม่มี Timeout
- ควบคุมได้เต็มที่

## สรุป

**สำหรับข้อมูลทั้งปี แนะนำ:**

1. 🥇 **ใช้ Local Development** - ง่ายที่สุด ไม่มี timeout
2. 🥈 **แบ่งช่วงทีละเดือน** - ใช้ได้กับ Vercel Free
3. 🥉 **ใช้ Script Python** - สำหรับผู้ที่ต้องการ automation

**ไม่แนะนำ:**
- ❌ ดึงข้อมูลทั้งปีบน Vercel Free (จะ timeout)
- ❌ ดึงข้อมูลหลาย Zone พร้อมกันในช่วงเวลายาว
