# 📖 วิธีใช้งานระบบยกเลิกรายการ Trade-In

## ขั้นตอนการตั้งค่าครั้งแรก (ทำครั้งเดียว)

### 1. ตั้งค่าข้อมูลพนักงาน
1. กดปุ่ม **⚙️ ตั้งค่าข้อมูลพนักงาน**
2. กรอกข้อมูล:
   - รหัสพนักงาน
   - ชื่อพนักงาน
   - เบอร์โทรศัพท์
   - เลือกประเภทการยกเลิก
   - เลือกสาเหตุการยกเลิก

### 2. ดึง Session ID (สำคัญ!)

#### 🌟 วิธีที่ 1: ใช้ Browser Extension (แนะนำ - อัตโนมัติ 100%)
1. ติดตั้ง Extension:
   - เปิด Chrome/Edge → ไปที่ `chrome://extensions/`
   - เปิด "Developer mode"
   - คลิก "Load unpacked"
   - เลือกโฟลเดอร์ `extension`
   
2. ใช้งาน:
   - Login ที่ https://eve.techswop.com/TI/login.aspx
   - **Session ID จะถูกดึงและบันทึกอัตโนมัติ!** ✨
   - คลิกไอคอน Extension → กด "📤 ส่งไปยังระบบ"
   - เสร็จแล้ว! ไม่ต้อง copy-paste เอง

3. ข้อดี:
   - ✅ ดึงอัตโนมัติทุกครั้งที่ login
   - ✅ อัพเดทอัตโนมัติเมื่อ Session เปลี่ยน
   - ✅ ส่งไปยังระบบได้ทันที
   - ✅ ไม่ต้องเปิด Console

#### วิธีที่ 2: ใช้ Bookmarklet (ง่าย)
1. ลากลิงก์นี้ไปวางที่ Bookmarks Bar: 
   ```
   📋 Get Session ID
   ```
2. เปิดเว็บ https://eve.techswop.com/TI/login.aspx
3. Login เข้าระบบ
4. คลิกที่ Bookmark "Get Session ID" ที่เพิ่งสร้าง
5. Copy Session ID ที่แสดงขึ้นมา
6. กลับมาที่ระบบนี้ → เปิดตั้งค่า → วาง Session ID
7. กดบันทึก

#### วิธีที่ 3: ใช้ปุ่มดึงอัตโนมัติในระบบ (ง่าย)
1. เปิดระบบ → กดปุ่ม **⚙️ ตั้งค่าข้อมูลพนักงาน**
2. ในส่วน Session ID → กดปุ่ม **🔄 ดึงอัตโนมัติ**
3. ระบบจะเปิดหน้า eve.techswop.com ให้อัตโนมัติ
4. Login เข้าระบบ
5. กด F12 → Console → วางคำสั่งที่ระบบ copy ให้แล้ว (Ctrl+V)
6. Copy ค่าที่ได้ → กลับมาวางในช่อง Session ID
7. กดบันทึก

#### วิธีที่ 4: ใช้ Console (สำหรับคนที่ชำนาญ)
1. เปิดเว็บ https://eve.techswop.com/TI/login.aspx
2. Login เข้าระบบ
3. กด F12 เปิด DevTools
4. ไปที่แท็บ Console
5. พิมพ์คำสั่ง:
   ```javascript
   document.cookie.match(/ASP\.NET_SessionId=([^;]+)/)[1]
   ```
6. Copy ค่าที่แสดงออกมา
7. กลับมาที่ระบบนี้ → เปิดตั้งค่า → วาง Session ID
8. กดบันทึก

## การใช้งานประจำวัน

### ค้นหาข้อมูล
1. เลือกวันที่ที่ต้องการค้นหา
2. เลือกตัวกรองอื่นๆ (ถ้าต้องการ)
3. กดปุ่ม **🔍 ค้นหา**

### ยกเลิกรายการ
1. ✅ เลือกรายการที่ต้องการยกเลิก (ติ๊กช่อง checkbox)
2. กดปุ่ม **🗑️ ยกเลิกรายการที่เลือก**
3. กรอกเหตุผลเพิ่มเติม (ถ้ามี)
4. ยืนยันการยกเลิก
5. รอผลลัพธ์

## ⚠️ หมายเหตุสำคัญ

- **Session ID จะหมดอายุ** เมื่อ logout หรือปิดเบราว์เซอร์
- ถ้ายกเลิกรายการไม่ได้ (ขึ้น Session หมดอายุ) ให้:
  1. Login ใหม่ที่ eve.techswop.com
  2. ดึง Session ID ใหม่
  3. อัพเดทในตั้งค่า
  4. ลองยกเลิกอีกครั้ง

## 🎯 Tips
- ใช้ปุ่ม "เลือกทั้งหมด" เพื่อเลือกรายการทั้งหน้า
- **ใช้ Extension จะสะดวกที่สุด** - Session ID อัพเดทอัตโนมัติทุกครั้งที่ login
- ข้อมูลพนักงานจะถูกบันทึกไว้ในเบราว์เซอร์ ไม่ต้องกรอกใหม่ทุกครั้ง
- Extension จะแจ้งเตือนเมื่อ Session ID อัพเดทสำเร็จ
