# 🚀 เริ่มใช้งานด่วน - ระบบยกเลิกรายการ Trade-In

## 📋 ขั้นตอนการติดตั้ง (ทำครั้งเดียว)

### 1. ติดตั้งระบบ
```bash
pip install -r requirements.txt
python app.py
```
เปิดเบราว์เซอร์ไปที่: http://localhost:5000

### 2. ติดตั้ง Extension (แนะนำ - อัตโนมัติ 100%)
1. เปิด Chrome → ไปที่ `chrome://extensions/`
2. เปิด **"Developer mode"**
3. คลิก **"Load unpacked"**
4. เลือกโฟลเดอร์ `extension`
5. Pin Extension ไว้ที่แถบเครื่องมือ

### 3. ตั้งค่าข้อมูลพนักงาน
1. กดปุ่ม **⚙️ ตั้งค่าข้อมูลพนักงาน**
2. กรอกข้อมูล: รหัส, ชื่อ, เบอร์โทร, ประเภท, สาเหตุ
3. กดบันทึก

---

## 🎯 การใช้งานประจำวัน

### วิธีที่ 1: ใช้ Extension (แนะนำ - ง่ายสุด!)
1. Login ที่ https://eve.techswop.com/TI/login.aspx
2. **Session ID จะถูกดึงอัตโนมัติ!** ✨
3. คลิกไอคอน Extension → กด **"📤 ส่งไปยังระบบ"**
4. เสร็จแล้ว! ไปใช้งานระบบได้เลย

### วิธีที่ 2: ใช้ปุ่มดึงอัตโนมัติ (ง่าย)
1. เปิดระบบ → กด **⚙️ ตั้งค่า**
2. กดปุ่ม **🔄 ดึงอัตโนมัติ** (ระบบจะเปิดหน้า eve.techswop.com ให้)
3. Login → กด F12 → Console → วางคำสั่ง (Ctrl+V)
4. Copy Session ID → กลับมาวางในช่อง → บันทึก

### วิธีที่ 3: ใช้ Console (แบบ Manual)
1. Login ที่ https://eve.techswop.com/TI/login.aspx
2. กด F12 → Console
3. พิมพ์: `document.cookie.match(/ASP\.NET_SessionId=([^;]+)/)[1]`
4. Copy ค่าที่ได้ → วางในตั้งค่าระบบ

---

## 📝 ขั้นตอนการยกเลิกรายการ

1. **ค้นหา**: เลือกวันที่ → กด 🔍 ค้นหา
2. **เลือก**: ✅ ติ๊กรายการที่ต้องการยกเลิก
3. **ยกเลิก**: กด 🗑️ ยกเลิกรายการที่เลือก
4. **ยืนยัน**: กรอกเหตุผล (ถ้ามี) → ยืนยัน
5. **เสร็จสิ้น**: รอผลลัพธ์

---

## ⚡ Tips

- ✅ **ใช้ Extension** = ไม่ต้อง copy-paste Session ID เอง
- ✅ **Session ID อัพเดทอัตโนมัติ** ทุกครั้งที่ login ใหม่
- ✅ ใช้ปุ่ม "เลือกทั้งหมด" เพื่อเลือกรายการทั้งหน้า
- ✅ ข้อมูลพนักงานบันทึกไว้แล้ว ไม่ต้องกรอกใหม่

---

## ⚠️ แก้ปัญหา

**Session หมดอายุ?**
- Login ใหม่ที่ eve.techswop.com
- Extension จะดึง Session ID ใหม่ให้อัตโนมัติ
- กด "📤 ส่งไปยังระบบ" อีกครั้ง

**ยกเลิกไม่ได้?**
- ตรวจสอบว่า Session ID ยังใช้งานได้
- ลอง login ใหม่และดึง Session ID ใหม่

---

## 📚 เอกสารเพิ่มเติม

- [วิธีใช้งานแบบละเอียด](วิธีใช้งาน.md)
- [คู่มือ Extension](extension/README.md)
- [API Specification](API_SPEC.md)
